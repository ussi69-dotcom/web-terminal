# DeckTerm Configuration

# Server port (default: 4174)
PORT=4174

# Bind address (default: 0.0.0.0)
HOST=0.0.0.0

# Debug logging (1 = enabled, 0 = disabled)
OPENCODE_WEB_DEBUG=0

# Maximum concurrent terminals (default: 10)
OPENCODE_WEB_MAX_TERMINALS=10

# Maximum terminals per user (default: 10)
MAX_TERMINALS_PER_USER=10

# Cloudflare Access Authentication
# Set CF_ACCESS_REQUIRED=1 to enable mandatory JWT authentication
CF_ACCESS_REQUIRED=1
CF_ACCESS_TEAM_NAME=your-team-name
CF_ACCESS_AUD=your-aud-tag

# CORS Security
# Comma-separated list of allowed origins (e.g., https://termdeck.eu,https://termdeck.cloudflareaccess.com)
# Leave empty to allow all origins (development only)
TRUSTED_ORIGINS=https://your-domain.com

# OpenCode Integration
# Backend proxy URL (for health checks, local server)
OPENCODE_UPSTREAM=http://127.0.0.1:4096

# Frontend URL (for iframe embedding - should be exposed via Cloudflare Tunnel)
# Leave empty to disable OpenCode panel, or set to your Cloudflare-exposed URL
# Example: https://opencode.yourdomain.com
OPENCODE_URL=
